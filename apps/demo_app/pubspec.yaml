name: demo_app
description: A demo app using workspace packages
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # ワークスペース内のパッケージを使用
  core_utils:
    path: ../../packages/core_utils
  ui_components:
    path: ../../packages/ui_components

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
