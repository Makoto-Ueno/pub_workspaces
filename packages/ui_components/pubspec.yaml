name: ui_components
description: Reusable UI components for the workspace
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
